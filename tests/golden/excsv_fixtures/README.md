# ExCSV `#chart` fixtures

Copied verbatim (CC0) from the [ExCSV spec](https://github.com/boligolov/excsv)'s own shared
fixture corpus — `fixtures/plain/{valid,invalid}/*chart*.excsv` — so our `#chart` support is
tested against the format's own ground truth, not just our own assumptions about it. See
`tests/test_excsv.py` for the expectations (adapted from `fixtures/fixtures.yaml` in that repo).
