# Copy to .env and adjust if you don't want the defaults baked into
# docker-compose.yml. Only used by `docker compose`; the server itself only
# reads DATABASE_URL directly (see docs/development.md).
POSTGRES_USER=ascii_charts
POSTGRES_PASSWORD=ascii_charts
POSTGRES_DB=ascii_charts
